require 'spec_helper'

describe AnAppleADay do
  it 'has a version number' do
    expect(AnAppleADay::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
